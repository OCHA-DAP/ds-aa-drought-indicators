[project]
name = "ds-aa-drought-indicators"
version = "0.1.0"
description = "Do temperature, rainfall, water balance and vegetation indicators predict drought impact across OCHA's drought AA framework countries?"
requires-python = ">=3.11"
dependencies = ["pandas>=2.2", "numpy>=1.26", "scipy>=1.12", "pyarrow>=15", "requests>=2.31", "ocha-stratus>=0.3", "sqlalchemy>=2.0", "psycopg2-binary>=2.9"]
